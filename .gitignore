# ── Python / Backend ─────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.pyw
*.pyz
.Python
*.egg-info/

.env
.env.local
.env.production

.env
.env.local
.env.production
venv/
env/
.venv/

# ML Models 
saved_models/*.pkl
saved_models/*.json

# ML Models (large binary files — regenerate on first run)
saved_models/*.pkl
saved_models/*.json

# ── Node / Frontend ───────────────────────────────────────

node_modules/
dist/
dist-ssr/
*.local

# ── VS Code 
.vscode/
*.code-workspace

# ── OS Files 
# ── VS Code ───────────────────────────────────────────────
.vscode/
*.code-workspace

# ── OS Files ──────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ─────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*

# ── Uploaded resumes (user data — never commit) ───────────
uploads/
temp/
.env.local
.env
package-lock.json
